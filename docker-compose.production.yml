version: '2'
services:
  web:
    image: unlimittt/demo-gatsby:production
    build:
      context: ./
      dockerfile: Dockerfile.production
    tty: true
    volumes:
      - ./:/usr/src/app
    ports:
      - "80:80"